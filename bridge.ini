[general]
;path name
log = {this_dir}/bridge.log
;data directory of bridge
dir = {this_dir}/.bridge

[model]
driver = pickle

[io]
services = insteon

[insteon]
name = insteon
protocol = insteon
file name = /dev/ttyUSB0
